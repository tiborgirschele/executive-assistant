import asyncio
async def run_api():
    print("==================================================", flush=True)
    print("🌐 EA OS API: ONLINE (Listening on Port 8090...)", flush=True)
    print("==================================================", flush=True)
    while True: await asyncio.sleep(3600)
